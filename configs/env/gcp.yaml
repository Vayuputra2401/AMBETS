# GCP training environment — Vertex AI / Cloud Storage
# Loaded when: python pipeline/train.py --env gcp

env: gcp

# GCP metadata (informational — printed at training start, not used by PyTorch code)
gcp:
  project:      "research-454007"
  bucket:       "ccr-brats-training"
  instance:     "ccr-research"
  zone:         "asia-east1-c"
  machine_type: "n1-standard-8"
  accelerator:  "nvidia-tesla-t4"

# Paths — bucket mounted at /gcs/<bucket> via gcsfuse
# checkpoint_dir uses local disk (/home) to avoid read-only scope issue — sync to GCS after training
paths:
  data_root:       "/gcs/ccr-brats-training/brats2024"
  checkpoint_dir:  "/home/user/checkpoints"
  pretrained_path: "/gcs/ccr-brats-training/pretrained/model_swinvit.pt"
